<?php
/*
login as root user by sudo MariaDB or sudo mysql


CREATE DATABASE 'nommies';
USE nommies;

CREATE TABLE gameTable (
    `gameid` varchar(10) COLLATE utf8mb4_unicode_ci NOT NULL,
    `gameObject` text COLLATE utf8mb4_unicode_ci NOT NULL,
    `scoreObject` text COLLATE utf8mb4_unicode_ci NOT NULL,
    `gameStarted` tinyint(1) NOT NULL DEFAULT 0,
    `gameHash` varchar(10) COLLATE utf8mb4_unicode_ci DEFAULT NULL,
    PRIMARY KEY (`gameid`)
);

*/

?>
